{
  "name": "@glyph/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint src --max-warnings 0",
    "pipeline:demo": "node --import tsx src/demo.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@glyph/application": "workspace:*",
    "@glyph/domain": "workspace:*"
  }
}
